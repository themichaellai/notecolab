- provide(:title, @user.name)
.row
  .span7
    h2.inline Classes
    span.inline-link= link_to 'All Classes', courses_path
    ul
      - @courses.each do |course|
        li
          = link_to course.name, course_path(course)
          - if @correct_user
            span.inline-link= link_to 'Leave', course_leave_path(course), class: 'leave'

    - if @user.notes.count > 0
      h2 Notes Created
      ul
        - @user.notes.each do |note|
          li
            = link_to note.title, course_note_path(note.course, note)
            |  (#{note.course.name})



  .span4.sidebar
    h2.inline
      = @user.name
    = gravatar_for @user
    br
    = @user.email
    br
    br
    - if @correct_user
      hr
      h3 Links
      a href=aces_link_courses_path Link to ACES
